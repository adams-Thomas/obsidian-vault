`docker start` - Run the container in detached mode (Won't see terminal output etc)
`docker run` - Run the container in attached mode (Will be able to see terminal output etc)

###### Interactive Mode
